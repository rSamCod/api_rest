{
  "name": "paquetes-api",
  "version": "1.0.0",
  "description": "REST API para gestión de paquetes en bodega",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node tests/api.test.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "nedb-promises": "^6.2.1"
  },
  "devDependencies": {
    "node-fetch": "^2.7.0"
  }
}
